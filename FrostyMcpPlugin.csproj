<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">
  <PropertyGroup>
    <Configurations>Developer - Debug;Release - Alpha;Release - Beta;Release - Final</Configurations>
    <Platforms>x64</Platforms>
    <TargetFrameworks>net48</TargetFrameworks>
    <AssemblyTitle>FrostyMcpPlugin</AssemblyTitle>
    <Product>FrostyMcpPlugin</Product>
    <Copyright>Copyright ©  2026</Copyright>
    <CodeAnalysisRuleSet>MinimumRecommendedRules.ruleset</CodeAnalysisRuleSet>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <UseWPF>true</UseWPF>
    <OutputType>Library</OutputType>
    <!-- Frosty Toolsuite root (folder that contains FrostyEditor / FrostySdk).
         Default assumes this repo lives at FrostyToolsuite/Plugins/FrostyMcpPlugin.
         Override with /p:FrostyRoot=... or edit this path (same idea as MagixGames/FrostyBlueprintEditor). -->
    <FrostyRoot Condition="'$(FrostyRoot)' == ''">$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\..\'))</FrostyRoot>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Developer - Debug|x64' ">
    <DebugSymbols>true</DebugSymbols>
    <OutputPath>bin\Developer\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
  </PropertyGroup>
  
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release - Alpha|x64' ">
    <OutputPath>bin\Release\Alpha\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <Optimize>true</Optimize>
  </PropertyGroup>
  
  <PropertyGroup Condition="'$(Configuration)|$(Platform)' == 'Release - Beta|x64'">
    <OutputPath>bin\Release\Beta\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <Optimize>true</Optimize>
  </PropertyGroup>
  
  <PropertyGroup Condition="'$(Configuration)|$(Platform)' == 'Release - Final|x64'">
    <OutputPath>bin\Release\Final\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <Optimize>true</Optimize>
  </PropertyGroup>

  <!-- YAML export: upstream FrostyToolsuite has no EbxYamlWriter, only EbxXmlWriter.
       Define FROSTY_HAS_YAML_WRITER when the Toolsuite in $(FrostyRoot) does provide
       FrostySdk\IO\EbxYamlWriter.cs whose constructor takes (Stream, AssetManager) -
       the signature Bridge/McpHandlers.cs calls.  Pass /p:FrostyHasYamlWriter=false
       to force it off for a Toolsuite whose writer has another signature (e.g.
       HarGabt/FrostyToolsuite takes (EbxAsset, Stream, AssetManager, int, bool)). -->
  <PropertyGroup>
    <FrostyHasYamlWriter Condition="'$(FrostyHasYamlWriter)' == '' And Exists('$(FrostyRoot)FrostySdk\IO\EbxYamlWriter.cs')">true</FrostyHasYamlWriter>
    <FrostyHasYamlWriter Condition="'$(FrostyHasYamlWriter)' == ''">false</FrostyHasYamlWriter>
  </PropertyGroup>
  <PropertyGroup Condition="'$(FrostyHasYamlWriter)' == 'true'">
    <DefineConstants>$(DefineConstants);FROSTY_HAS_YAML_WRITER</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="PresentationCore" />
    <Reference Include="PresentationFramework" />
    <Reference Include="System.Xaml" />
    <Reference Include="WindowsBase" />
    <!-- Bridge/Mcp*Handlers.cs use `dynamic`, which needs the Microsoft.CSharp
         binder. Without an explicit reference the build fails with CS0656
         (Missing compiler required member ...CSharpArgumentInfo.Create) during
         the WPF markup-compile temp-project pass. -->
    <Reference Include="Microsoft.CSharp" />
    <!-- Frosty binaries / third-party (adjust FrostyRoot if your Toolsuite is elsewhere) -->
    <Reference Include="Newtonsoft.Json">
      <HintPath>$(FrostyRoot)FrostyEditor\ThirdParty\Newtonsoft.Json.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="$(FrostyRoot)FrostyControls\FrostyControls.csproj">
      <Private>false</Private>
    </ProjectReference>
    <ProjectReference Include="$(FrostyRoot)FrostyHash\FrostyHash.vcxproj">
      <Private>false</Private>
    </ProjectReference>
    <ProjectReference Include="$(FrostyRoot)FrostyPlugin\FrostyCore.csproj">
      <Private>false</Private>
    </ProjectReference>
    <ProjectReference Include="$(FrostyRoot)FrostySdk\FrostySdk.csproj">
      <Private>false</Private>
    </ProjectReference>
  </ItemGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <PropertyGroup>
      <_FrostyEditorPluginsDir>$(FrostyRoot)FrostyEditor\$(OutDir)Plugins\</_FrostyEditorPluginsDir>
    </PropertyGroup>
    <MakeDir Directories="$(_FrostyEditorPluginsDir)" />
    <Copy SourceFiles="$(TargetPath)" DestinationFolder="$(_FrostyEditorPluginsDir)" SkipUnchangedFiles="true" />
  </Target>

</Project>
